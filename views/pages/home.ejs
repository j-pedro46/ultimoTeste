<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ofícios Online</title>
    <link rel="shortcut icon" href="logo.png" type="image/x-icon">

    <!-- Fonte moderna -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --azul: #2563EB;
            --azul-hover: #1D4ED8;
            --bg-escuro: #0F172A;
            --bg-card: #1E293B;
            --texto: #FFFFFF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: linear-gradient(135deg, #0F172A, #1E3A8A);
        }

        nav {
            background: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(8px);
            padding: 15px 30px;
            display: flex;
            justify-content: center;
            gap: 30px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        nav a {
            color: var(--texto);
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
        }

        nav a:hover {
            color: var(--azul);
        }

        main {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 30px;
        }

        .painel {
            background: var(--bg-card);
            padding: 50px 40px;
            border-radius: 16px;
            text-align: center;
            box-shadow: 0 10px 35px rgba(0,0,0,0.6);
            max-width: 500px;
            width: 100%;
        }

        .logo img {
            width: 180px;
            margin-bottom: 20px;
        }

        h1 {
            color: var(--texto);
            font-size: 28px;
            margin-bottom: 10px;
        }

        p {
            color: #CBD5F5;
            font-size: 14px;
            margin-bottom: 25px;
        }

        button {
            background: var(--azul);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: 0.3s;
        }

        button:hover {
            background: var(--azul-hover);
            transform: scale(1.03);
        }

        footer {
            text-align: center;
            padding: 20px;
            background: rgba(15, 23, 42, 0.9);
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        footer p {
            color: #94A3B8;
            font-size: 12px;
        }
    </style>
</head>
<body>

    <nav>
        <a href="#">HOME</a>
        <a href="/cadastro">CADASTRO</a>
        <a href="/lister">LISTA</a>
        <a href="/logout">SAIR</a>
    </nav>

    <main>
        <div class="painel">
            <div class="logo">
                <img src="/img/logocamara.png" alt="Logo Câmara">
            </div>
            <h1>OFÍCIOS ONLINE</h1>
            <p>Bem-vindo ao sistema de gerenciamento de ofícios da Câmara Municipal.</p>
            <button onclick="location.href='/cadastro'">Cadastrar Novo Ofício</button>
        </div>
    </main>

    <footer>
        <p>Paraipaba • Ceará • Brasil • 2025</p>
        <p>Sistema de Publicação de Ofícios da Câmara Municipal</p>
        <p>© 2025 - Ofícios Online | Desenvolvido por João Pedro</p>
    </footer>

</body>
</html>