<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Lista de Ofícios</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --azul: #2563EB;
            --azul-hover: #1D4ED8;
            --bg-escuro: #0F172A;
            --bg-card: #1E293B;
            --texto: #FFFFFF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: linear-gradient(135deg, #0F172A, #1E3A8A);
        }

        main {
            flex: 1;
            padding: 30px 0;
        }

        /* CONTAINER PADRÃO DO SISTEMA */
        .container-sistema {
            width: 95%;
            max-width: 1200px;
            margin: auto;
        }

        nav {
            background: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(8px);
            padding: 15px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .nav-content {
            display: flex;
            justify-content: center;
            gap: 30px;
        }

        nav a {
            color: var(--texto);
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
        }

        nav a:hover {
            color: var(--azul);
        }

        h2 {
            text-align: center;
            margin: 20px 0;
            color: var(--texto);
        }

        .tabela-box {
            background: var(--bg-card);
            border-radius: 14px;
            padding: 25px;
            width: 100%;
            box-shadow: 0 10px 35px rgba(0, 0, 0, 0.6);
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            color: #CBD5F5;
        }

        th {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        td {
            padding: 12px;
        }

        tr:hover {
            background: rgba(255, 255, 255, 0.05);
        }

        .btn-delete,
        .btn-update {
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 12px;
            text-decoration: none;
            font-weight: 600;
            margin-right: 5px;
        }

        .btn-delete {
            background: #EF4444;
            color: white;
        }

        .btn-update {
            background: var(--azul);
            color: white;
        }

        .btn-delete:hover {
            background: #DC2626;
        }

        .btn-update:hover {
            background: var(--azul-hover);
        }

        #overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(5px);
            justify-content: center;
            align-items: center;
        }

        #editarBox {
            background: var(--bg-card);
            padding: 30px;
            border-radius: 14px;
            width: 90%;
            max-width: 400px;
            color: white;
            box-shadow: 0 10px 35px rgba(0, 0, 0, 0.6);
        }

        #editarBox input {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border-radius: 6px;
            border: none;
        }

        #editarBox button {
            margin-top: 10px;
            padding: 10px;
            border-radius: 6px;
            border: none;
            font-weight: 600;
            width: 100%;
            cursor: pointer;
        }

        #salvarBtn {
            background: var(--azul);
            color: white;
        }

        #cancelarBtn {
            background: #EF4444;
            color: white;
        }

        footer {
            text-align: center;
            padding: 20px;
            background: rgba(15, 23, 42, 0.9);
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        footer p {
            color: #94A3B8;
            font-size: 12px;
        }
    </style>
</head>

<body>

    <nav>
        <div class="container-sistema">
            <div class="nav-content">
                <a href="/home">HOME</a>
                <a href="/cadastro">CADASTRO</a>
                <a href="#">LISTA</a>
                <a href="/logout">SAIR</a>
            </div>
        </div>
    </nav>

    <main>
        <div class="container-sistema">

            <h2>Lista de Ofícios</h2>

            <div class="tabela-box">
                <table>
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Número</th>
                            <th>Descrição</th>
                            <th>Data</th>
                            <th>Ações</th>
                        </tr>
                    </thead>

                    <tbody>
                        <% if (oficios && oficios.length> 0) { %>
                            <% oficios.forEach(o=> { %>
                                <tr>
                                    <td>
                                        <%= o.nome %>
                                    </td>
                                    <td>
                                        <%= o.numero %>
                                    </td>
                                    <td>
                                        <%= o.descricao %>
                                    </td>
                                    <td>
                                        <%= new Date(o.data_emissao).toLocaleDateString("pt-BR") %>
                                    </td>

                                    <td>
                                        <a href="/oficio/<%= o.id %>" class="btn-delete">Apagar</a>
                                        <a href="#" class="btn-update"
                                            onclick="abrirEditar('<%= o.id %>', '<%= o.nome %>', '<%= o.numero %>','<%= o.descricao %>', '<%= o.data_emissao %>')">
                                            Editar
                                        </a>
                                    </td>
                                </tr>
                                <% }) %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="4" style="color:#EF4444;">Nenhum ofício cadastrado.</td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>

        </div>

        <div id="overlay">
            <div id="editarBox">
                <form id="formEditar" method="POST">
                    <input type="text" name="nome" id="editNome" placeholder="Nome">
                    <input type="text" name="numero" id="editNumero" placeholder="Número">
                    <input type="date" name="data_emissao" id="editData">
                    <button id="salvarBtn" type="submit">Salvar</button>
                    <button id="cancelarBtn" type="button" onclick="fecharBox()">Cancelar</button>
                </form>
            </div>
        </div>

    </main>

    <footer>
        <p>Paraipaba • Ceará • Brasil • 2025</p>
        <p>Sistema de Publicação de Ofícios da Câmara Municipal</p>
        <p>© 2025 - Ofícios Online | Desenvolvido por João Pedro</p>
    </footer>

    <script>
        function abrirEditar(id, nome, numero, descricao, data) {
            document.getElementById("overlay").style.display = "flex";
            document.getElementById("editNome").value = nome;
            document.getElementById("editNumero").value = numero;
            document.getElementById("editDescricao").value = descricao;
            document.getElementById("editData").value = data.substring(0, 10);
            document.getElementById("formEditar").action = "/oficio/editar/" + id;
        }
        function fecharBox() {
            document.getElementById("overlay").style.display = "none";
        }
    </script>

</body>

</html>